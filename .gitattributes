# 統一以 LF 儲存，避免在 Windows 與 Linux 之間產生換行雜訊。
* text=auto eol=lf

# 二進位檔案不做換行轉換。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary

# 產生外掛壓縮檔（GitHub 的 Download ZIP／git archive）時排除開發用檔案。
/.gitattributes  export-ignore
/.gitignore      export-ignore
/.github/        export-ignore
/tests/          export-ignore
/README.md       export-ignore
